const BASE_URL = "http://localhost:3000/";

const VERSION = "v1";

export const API_URL = `${BASE_URL}${VERSION}`;

export const GET_PRODUCTS_URL = `${API_URL}/products`;

export const GET_PURCHASE_OPTIONS_URL = `${API_URL}/purchase?`;

export const GET_ORDERS_URL = `${API_URL}/orders`;
export const CREATE_ORDER_URL = `${API_URL}/orders`;